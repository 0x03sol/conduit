# Conduit indexer environment template.
# Copy to .env.local before running.

# Arc testnet RPC (public, no key needed).
PONDER_RPC_URL_ARC_TESTNET=https://rpc.testnet.arc.network

# Optional: pin the GraphQL port (default 42069).
PORT=42069
